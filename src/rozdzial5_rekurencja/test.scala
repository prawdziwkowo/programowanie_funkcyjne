//w scali val - to zmienne niemutowalne, var - to zmienne mutowalne

/*
class Rekurencja {
  def sumaRekurencyjna(x: Integer, sum: Integer = 0): Integer = {
    if (x == 0) {
      return sum
    }
    return sumaRekurencyjna(x - 1, sum + x)
  }
}

var rec = new Rekurencja()
var test = rec.sumaRekurencyjna(10)
println(test)
*/